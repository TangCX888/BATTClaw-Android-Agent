# Android Apk 安装包文件夹
### 默认安装位置
`ProjectRoot/assets/apk/`

### 必须安装 
位置：`./apk` 
1. `adbKeyboard.apk` -  adb 输入法
2. `sougou.apk` - 搜狗输入法 - 后期方便用户操作
3. `via.apk` - via浏览器

### 用户自装文件夹 
位置：`./other` 

说明：用户未来自行增加的 apk 文件，只要文件里面有 apk 包就会自动安装，需要设置初始化包方法，当用户需要初始化应用的时候自动移动到这里